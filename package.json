{
  "name": "ask-more",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "start": "node server.js",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,yml,yaml,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@babel/preset-env": "^7.26.0",
    "eslint": "^10.4.1",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "jest": "^29.7.0",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.3"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
